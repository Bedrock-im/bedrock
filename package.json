{
  "name": "bedrock",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "yarn workspace front run lint",
    "format": "yarn workspaces foreach -A run format",
    "format:check": "yarn workspaces foreach -A run format:check"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "yarn@4.5.0",
  "workspaces": [
    "front",
    "docs"
  ],
  "dependencies": {
    "ts-node": "^10.9.2"
  }
}
